export const speechService={isSupported:()=>typeof speechSynthesis!=='undefined',speak:(text:string,rate=.9)=>{if(!speechService.isSupported())return false;speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang='pt-BR';u.rate=rate;speechSynthesis.speak(u);return true},stop:()=>speechSynthesis?.cancel(),pause:()=>speechSynthesis?.pause(),resume:()=>speechSynthesis?.resume()}
