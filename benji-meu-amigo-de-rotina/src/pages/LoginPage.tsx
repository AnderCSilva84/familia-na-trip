import{useForm}from'react-hook-form';import{zodResolver}from'@hookform/resolvers/zod';import{z}from'zod';import{Navigate,useNavigate}from'react-router-dom';import{BenjiCharacter}from'../components/BenjiCharacter';import{Button,Card}from'../components/ui';import{useAuth}from'../contexts/AuthContext'
const schema=z.object({email:z.email('Informe um e-mail válido'),password:z.string().min(6,'Use pelo menos 6 caracteres')});type Form=z.infer<typeof schema>
export default function LoginPage(){const{user,login,devMode}=useAuth();const nav=useNavigate();const{register,handleSubmit,formState:{errors,isSubmitting}}=useForm<Form>({resolver:zodResolver(schema)});if(user)return<Navigate to="/responsavel"/>;return<main className="auth-page"><BenjiCharacter size="large" animated message="Olá! Eu sou o Benji." showSpeechBubble/><Card><h1>Benji</h1><p>Meu Amigo de Rotina</p>{devMode?<p className="notice">Modo local de desenvolvimento</p>:null}<form onSubmit={handleSubmit(async v=>{await login(v.email,v.password);nav('/responsavel')})}><label>E-mail<input type="email" {...register('email')}/><small>{errors.email?.message}</small></label><label>Senha<input type="password" {...register('password')}/><small>{errors.password?.message}</small></label><Button disabled={isSubmitting}>Entrar com tranquilidade</Button></form><button className="link" onClick={()=>nav('/recuperar-senha')}>Esqueci minha senha</button></Card></main>}
